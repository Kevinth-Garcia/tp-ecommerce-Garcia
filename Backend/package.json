{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    
    "start":"json-server --watch db.json --port 3001",
    "dev": "json-server --watch db.json --port 3001"
  },
  "keywords": [],
  "author": "Kevinth Garcia",
  "license": "ISC",
  "description": "JSON Server backend for ecommerce application Se usa el comando 'Start' con node.js para iniciar la aplicacion mientras con el 'Watch' se ven los cambios y se reinicia automaticamente" ,
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
